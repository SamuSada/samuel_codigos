"use strict";
for (var hora = 0; hora <= 23; hora++) {
    if (hora >= 8 && hora <= 22) {
        console.log(`Son las ${hora}:00, CUCÚ!`);
    } else {
        console.log(`Son las ${hora}:00`);
    }
}